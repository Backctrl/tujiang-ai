# M2 已验收分包记录

日期：2026-09-07。2A1、2A2、2B1、2B2、2D1、2D2 的限定工程包已验收。用户指出 Setup 中间区域与连续表单参考不一致，且主启动交互尚未完整接通，因此重新打开 2C 整页验收，新增 2A3、2C2/2C3 补齐。此前配置版本和恢复证据保留，但不再据此声称 Setup 整页完成。M2 整体未关闭。`businessAcceptance=false`，真实产品事实、官方规则目录及资料用途尚未由本记录批准。

## 提交与验证

| 包 | 最终审查版本 | 合并回执与结果 |
| --- | --- | --- |
| 2A 正式上下文与规则绑定 | `fec0b0e` | [PR #9](https://github.com/Backctrl/tujiang-ai/pull/9)，`1885d8cc126c16be4c985515d63fe255f98052ed`；领域、HTTP、真实 PostgreSQL 与独立审查通过 |
| 2B1 原件接收与解析 | `fb002475ccbd703eb46e08e9a78045222bbb5fdf` | [PR #11](https://github.com/Backctrl/tujiang-ai/pull/11)，`62ffc2265a07c0e551fb97b55958b53bcfdc879c`；后端 91 项、PostgreSQL 11 项、类型与构建通过 |
| 2C 配置版本与恢复子集 | `f132850534df0bea1390e2114f309a0cbe279c7f` | [PR #12](https://github.com/Backctrl/tujiang-ai/pull/12)，`cf36eea129e0fc60929e3864d6c22ce64ccf3c73`；限定客户端／恢复检查通过；连续表单与最终启动流程未覆盖，2C 整包重新验收 |
| 2B2 用途纠正与来源重确认 | `1b53e85a30f10d21b828727f8ab804f423634532` | [PR #14](https://github.com/Backctrl/tujiang-ai/pull/14)，`84294afaa95cf522b3bcc91a2ffe7b8001774ec4`；114 项后端／13 项真实 PostgreSQL 与独立审查通过 |
| 2D1 上传、解析与恢复 | `6f6a216d0cda84e5e55b3cb4058b60efcee47acc` | [PR #15](https://github.com/Backctrl/tujiang-ai/pull/15)，`dad9fff4e98b201a71409075295d326d9c491315`；恢复与原件证据见下，含人工证据限额修复 |
| 2D1 Facts 素材卡片修复 | `729e165c148e85fcf1870ab15d0697aa9c367051` | [PR #16](https://github.com/Backctrl/tujiang-ai/pull/16)，`ca45fa6778b31fd5910583051b2eb010684ac8a5`；真实多卡／展开／分页／下载与 Q 独立验收通过 |
| 2A2 范围化规则与兼容 | `aefb022444de3014fe0de1c110413ce7e2bfdf32` | [PR #18](https://github.com/Backctrl/tujiang-ai/pull/18)，`27243e6213b6d653506c2d9b381528412b8910b0`；范围检查、历史不可变、真实 PostgreSQL 与浏览器兼容检查通过 Q |
| 2D2 用途审核与来源处理界面 | `b97a63e839f59aee548b4d0ec03d20ed9092ca44` | [PR #20](https://github.com/Backctrl/tujiang-ai/pull/20)，`c112d72b9ebb77e1f9f608ba3432f81b73ebf5ca`；Q 34/34、三项 CI、真实 PostgreSQL／浏览器四类任务及恢复链通过，两项 P2 已关闭 |

PR #11、#12、#14、#15、#16 的 Windows、Ubuntu 和独立 PostgreSQL CI 均通过。前端 lint 保留原两项 warning。原六阶段布局及 `arcane-warrior.css` 未改动；材料区样式扩展位于局部集成 CSS。

2B1 支持经过本包检查的 UTF-8 TXT、Markdown、CSV、JSON 与单帧 PNG、JPEG、WebP。原件落盘并校验 SHA-256；解析块保留原件指针、parserVersion 和文字／单元格／JSON 位置。单文件失败隔离、重复请求、Worker 中断恢复、JSONB 往返和完整图片解码由测试覆盖。重复来源字段比较和 APNG 多帧漏检在独立审查后修复。本包只生成待审核材料块，不创建正式事实、素材或章节；PDF、Office 与 OCR 尚不在支持清单。

## 2C 真实浏览器证据

集成浏览器使用 1440×1000 Chromium、原 `/arcane-warrior`、真实 Fastify 和 PostgreSQL。隔离 schema 为本次 `mvp_browser_*`，两个应用共享此存储：4312 使用显式合成规则，4313 使用真实空目录；前端分别代理于 5182、5183。没有启动模型 Gateway。浏览器执行由主 Agent 完成，Q 独立核对最终代码、6 份脚本与 6 张截图。

| 场景 | 已观察行为 |
| --- | --- |
| 部分草稿与启用 | 仅填部分字段可保存，字段或规则缺失阻止启用；补齐后明确启用 P1 |
| 历史版本 | P1 只读，明确复制到新草稿、保存、启用 P2；P1 原快照逐字段保持不变，无正式 Facts／Section 自动生成 |
| 本地恢复 | A/B 草稿分别保留；重新打开页面需重新输入凭据；恢复旧草稿仍使用原起草依据，服务端已变时必须复核 |
| 空规则 | 完整上下文能保存为草稿；前端禁止启用；真实 API 返回 409 `RULE_PACK_UNAVAILABLE`，不增加版本或 revision |
| 旧回执晚到 | 真实保存已提交但暂缓回执，再执行较新配置变更，SSE 先更新页面；未决写仍锁住，旧回执到达后本地输入保留并要求复核 |
| 无关版本变化 | 较新的身份修改不改变当前配置含义；旧配置回执返回后允许正确清理已保存的本地草稿 |
| 同 revision 回读 | 等待 GET response、body 完成、刷新完成提示及 busy 清空后再断言；本地编辑保留，无 JSON 键顺序误报且能保存 |
| 页头与隔离 | 按已启用版本显示首发平台／站点／语言；只有服务端草稿显示“草稿未启用”，空配置显示“尚未配置”，项目切换同步更新 |

Q 发现的 JSON 属性顺序误判和 React 同批快照／回执竞态由 `21cc726` 修复；前者还有 PostgreSQL 浏览器证据，后者同批边界以独立 Hook 回归为证，浏览器验证的是可控制的晚到次序，不声称强制 React 精确同批。`f132850` 修复原页头仍显示未配置的问题。Q 当时对上述检查范围给出可合并结论；用户后续的整页布局与启动反馈证明验收范围不足，2C 现重新开放。

探针保留在 `F:/Project/tujiang-ai/03-runtime/tujiang-mvp-runtime/.playwright-cli/`，名称为 `m2-context-lifecycle-check.js`、`m2-context-recovery-check.js`、`m2-context-empty-rules-check.js`、`m2-context-save-order-check.js`、`m2-context-header-get-check.js`、`m2-context-draft-header-check.js`。它们依赖本次隔离项目状态；不能直接对任意业务库重复执行。关键截图：

- [首发目标与刷新后本地草稿](m2-context-header-get.png)
- [新快照先到、旧回执后到的复核](m2-context-late-receipt-review.png)

## 2B2 后端验收

Q 完整检查 15 文件差异与 `backend/MATERIAL-USAGE.md`，独立运行用途专测 11/11、真实 PostgreSQL 13/13、类型检查与 diff 检查，通过。逐块审核只接收当前 blockId 和用途，来源、定位、原件哈希和派生对象由服务端产生。参考改为证据会创建新 Evidence；旧候选与批准不复活。撤回证据用途保留锁定事实原值，并要求来源重确认；影响范围只标记相关依赖 stale。重确认仅接受同原件同块的新有效 Evidence，不自动恢复 stale。手工证据仍使用独立限额。

## 2D1 已完成的浏览器验证

主 Agent 在同一真实 PostgreSQL／Fastify 环境执行；Q 独立阅读脚本、代码和截图。以下验收为工程事实，不形成真实产品质量签收。

| 场景 | 已观察行为 |
| --- | --- |
| 七种支持格式与拒绝隔离 | 新建 E 项目依次上传 TXT、Markdown、CSV、JSON、PNG、JPEG、WebP 和 PDF；七份支持原件全部解析成功，PDF 仅一次 415，后续文件继续 |
| 解析进度并发 | E 的 Markdown 与 JSON 各遇真实解析进度 409；确认只有既有文件的 parser 事件后，各最多替换一次持久请求并使用新 key 提交成功 |
| 文件和项目隔离 | 选择、拖拽、切换项目和重新打开不会自动 POST；File 字节与来源留在本项目 IndexedDB；凭据不持久化 |
| 结果未知 | 实际服务端 200 后丢失回执，页面暂停新写入与切换；重开恢复原 body/key，先显式 GET 再原请求重试，服务端只保存一份原件 |
| 浏览器存储失败 | 保存恢复记录的真实 IndexedDB 事务失败时 0 POST；200 后清理 pending 失败时保留精确请求，重开后可核对和幂等回放 |
| 业务版本冲突 | 请求发送前由真实 API 修改上下文，上传返回 409 后不自动更新 key；读取／复核不自动上传，明确重试才以新意图提交 |
| 401 | 对实际上传请求注入无效 Authorization 头，真实后端 401；恢复有效请求头后使用原 body/key 成功。此探针未比较输入框旧值与新值不同的情况 |
| 单文件解析重试 | 损坏 JSON 明确 `INVALID_JSON` 且 0 块，后续 TXT 成功；重试只增加该文件尝试次数，其他八份成功原件保持 attempt=1 |
| 人工补证限额 | 修复派生 Evidence 误占 10 份人工名额；实际页面在 12 条派生证据时接受 1 份人工补证，Facts 仍显示全部 13 条。Q 实际组件另覆盖 10 份人工时禁用 |
| 设置页窄栏 | 1440×1000、1200×900 已目视；素材单列、长错误码换行，卡片无横向溢出 |

Q 最终补丁材料客户端回归 21/21 通过，其他会话／上下文恢复的对应回归和三平台 CI 已通过。解析进度换 key 后丢回执的组合由真实 HTTP 客户端测试验证；浏览器分别验证真实换 key 路径和 IndexedDB 事务／重开，不宣称另做了一遍相同组合。

最后额度截图发现 Facts 左侧滚动栏直接排列素材卡片时被 grid 压缩、内容相互覆盖。此问题是在 PR #15 合并后的最终截图核对中确认，由 PR #16 只修改该栏的纵向布局，子项按内容高度排列并保持独立滚动。真实 E 项目的 9 张折叠卡片各高约 244.78 px、间隔 9 px；首卡展开来源和候选后增加约 513.19 px，后卡等量顺移。1440 px 与 1200 px 下无内容溢出或互叠，实际下载哈希相等；AW 476 块候选的 24 页可前后翻页。主 Agent 和 Q 均目视全部四张截图，Q 最终 PASS，无剩余 P1/P2。2D1 工程验收据此关闭。

脚本位于本地 `F:/Project/tujiang-ai/03-runtime/tujiang-mvp-runtime/.playwright-cli/`：`m2-material-batch-v2-check.js`、`m2-material-local-queue-check.js`、`m2-material-uncertain-reopen-check.js`、`m2-material-parse-and-auth-check.js`、`m2-material-storage-and-business-check.js`、`m2-manual-evidence-quota-check.js`、`m2-facts-material-layout-check.js`。它们依赖专用合成项目与一次性故障注入，不能直接对业务库重复执行。

- [设置页批量上传完成](m2-material-batch-v2-1440.png)
- [Facts 多份原件顺排](m2-facts-materials-closed-1440.png)
- [1200 px 展开卡片与后续卡片](m2-facts-materials-open-1200.png)

## 2D2 四类审核与真实恢复链

限定范围为原 Facts 页的用途待审核、待提取候选、事实待确认、来源待重确认四类任务。主 Agent 在精确候选 `b97a63e` 的集成版本 `f580703` 上，用真实 Fastify、PostgreSQL 与 IndexedDB 验证；Q 独立检查实现、脚本、截图，并只读核对最终数据库状态。

四任务样本位于独立 `mvp_review_*` schema，两个原件均为明确合成材料。仅为建立诊断依赖，使用一次绑定该合成项目的本地合成规划器；外部模型 POST 为 0，未运行正式故事规划，未写入 AW 项目。

| 场景 | 已观察行为 |
| --- | --- |
| 四类任务与用途依赖 | 样本同时展示 2／1／1／1 项任务，引用来源和状态一致；参考文字不变成产品事实证据，图片仅作为素材／参考候选 |
| 撤回、恢复、来源重确认 | 三次明确 UI 写入使主链 R18→R21；影响预览与实际相关 Fact、候选和诊断依赖一致，来源恢复产生新 Evidence，不复活旧候选 |
| 锁定与历史 | 已锁定的 `10 kg` 原值、原人工确认和原件解析块均保留；新 Evidence 仅替换同原件同块的来源，追加一条重确认历史，无关 steel 候选不变 |
| 下游状态 | 原诊断 Section 和 Storyboard 保留且为 stale；重确认不自动恢复旧输出，也不生成正式章节 |
| 人工补证重复保护 | 修复名称／定位 trim 差异和重开后旧表单缺少回调两项 P2。真实服务端 200 后丢回执，重开先显式 GET，再以原 body/key 重放；只有一份证据，旧表单可核对但禁止再以新键重复保存 |
| 草稿隔离与重开 | A/B 项目、两份原件用途、人工候选分别保留；切换、选择、填写和重开均 0 次自动业务 POST；凭据不进入本地持久化 |
| 本地恢复存储失败 | 实际 IndexedDB pending 写事务失败时 0 POST，服务端保持 R10；恢复浏览器存储后才允许明确重试 |
| 真实 409 | 并发用途操作使 A R10→R11，原请求返回 409 且本地意图保留。读取、全局差异复核、局部来源依据复核均 0 POST；明确再次保存才用新 key 基于 R11 写到 R12 |
| 无关对象保持 | 409 恢复后 A 第二份原件和人工候选草稿保留，B 草稿与服务端 R10 不变；无关候选不被要求重新审核来源 |

恢复样本 A 为 `17787697-308e-4fbb-b0e5-57b431615681`，B 为 `998b95db-37cf-43f8-b8c4-738998e0e954`；四任务主链为 `c473232a-7644-450c-b453-95ece51238f5`。Q 最后另做三个只读 GET，确认 A R12、B R10、主链 R21 及上述历史、原块、用途和 stale 状态。脚本在迁移后的本地 `.playwright-cli/`：`m2-review-chain-check.js`、`m2-review-recovery-isolation-check.js`、`m2-manual-evidence-reopen-check.js`。前两类样本各有明确前置状态，不能直接对业务库执行；恢复脚本每次创建新的合成 A/B，并等待本次上传的 materialId 解析完成后才固定初始版本。

[四类任务与来源待重确认，1440 px 合成验收](m2-review-four-tasks-source-ready-1440.png)。该截图证明本包的四类任务布局，不代表完整 Facts 页或正式事实放行已经完成。Q 最终结论为本包 PASS，无未关闭 P1/P2。

## AW FLEX 实际原件验证

专用本地整理项目 `97506512-bfbc-467d-8a56-6b191a10d437` 接收四份上游 JSON/JPEG、两份逐字 CSV 导出及一份导出清单。资料用途全部保持待审核，`evidence=[]`、`facts=[]`，没有启用正式目标。原文修订分别记录文档 25、状态表 15；画板 API 无版本，未冒用文档版本。

| 文件 | 接收／解析结果 |
| --- | --- |
| document-rev25.json | 原件 1,431 bytes，JSON 解析 9 块 |
| review-table-rev15.json | 原件 2,448 bytes，JSON 解析 39 块 |
| whiteboard-raw.json | 原件 695,039 bytes 保留；明确 `PARSE_OUTPUT_LIMIT`，0 块，无部分结果冒充成功 |
| hero-reference.jpg | 原件 3,244,992 bytes，完整解码 1562×8071 JPEG，1 块 |
| review-table-rev15.export.csv | 逐字导出 1,162 bytes，22 行保留；清单记录父 JSON 哈希与去除行号前缀的转换 |
| whiteboard-text.export.csv | 逐字导出 27,579 bytes，475 文字节点加表头共 476 块；保留 nodeId、parentId 和顺序 |
| structured-export-manifest.json | 本地父子文件哈希清单 1,210 bytes，18 块；明确不是飞书版本或人工事实签收 |

七份入库 SHA-256 逐一与本地输入一致。大图、状态表 CSV、画板 CSV 通过页面下载后再次与输入校验 SHA-256，主 Agent 和 Q 独立核对均相等。首次批次中大图遇两波 parser 更新，耗尽一次自动同步额度后正确暂停；核对差异只包含原画板解析 started/failed，再明确重试大图并继续剩余文件。结果为七份导入文件完整保留、六份解析成功、一份超限明确失败，不能写成七份全部解析成功或全程无需复核。

执行脚本为 `m2-aw-original-intake-check.js` 与 `m2-aw-original-resume-check.js`。[原件与确定性导出来源截图](m2-aw-real-originals.png)。原始文件与确定性转换清单留在 Git 忽略的输入目录，未上传原始产品大图到仓库。

## 未关闭范围

Setup 必须按用户确认的功能分布完成整页验证：左侧五个锚点，中间单列连续表单，右侧启动检查、资料用途说明及分开的阻断问题和建议。资料标签属于系统预分类，待确认／混合／高风险用途进入 Facts 待处理中心；不要求员工在 Setup 逐文件批准用途。主操作为“创建项目并开始事实提取”，由服务端检查与原子启动支撑。平台、站点、语言、货币、单位和 RulePack 只展示已支持组合；CanvasProfile 在此创建，后续章节只读全局宽度。保留岳凯视觉系统；参考图中的样例文件和宽度不构成已支持类型或实际平台规则。

2D2 已关闭，但完整 Facts 页仍依赖 3A1/3A2 与 3C1：多来源对照、缺口处理、当前事实的 Agent 解释、左栏提取记录及正式事实版本放行均需继续实现与验收。2C2 的主表单本地化输入仍可组合出目录不支持的语言／货币／单位，是独立审查确认的 P2；修复前不合并 PR #21。2C3 还需把首次上传所需的内部准备步骤合进明确上传／主操作，并完成真实启动及恢复链。

目录迁移后重新打开原数据库容器并只读核对 AW 项目，仍为 R23、7 份原件、6 份解析成功与 1 份 `PARSE_OUTPUT_LIMIT`，`facts=[]`、`evidence=[]`、`runs=[]`。本次目录整理和合成审核没有改变 AW 的业务状态。

淘宝规则尚无本轮可用的官方证据；Amazon A+ 已核对部分模块要求。2A2 提供了范围化规则机制，不代表真实规则目录或业务签收已完成。单 SKU 具体型号、预期事实、真实业务签收和模型质量成绩仍未补齐；模型 POST 保持 0 次。
